= form_tag(users_path, :method => "get", id: "search-form") do
  = text_field_tag :search, params[:search], placeholder: "Search Users", data: {autocomplete_source: User.order(:username).map(&:username)}, class: 'inline-block'
  = submit_tag "Search", :username => nil, class: 'inline-block'
